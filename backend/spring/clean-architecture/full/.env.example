# Database Configuration
DB_NAME=apptemplate_dev
DB_USER=postgres
DB_PASSWORD=postgres

# JWT Configuration (minimum 32 characters for HS256)
JWT_SECRET=your-256-bit-secret-key-minimum-32-characters-long-for-hs256
JWT_EXPIRATION_MS=900000
JWT_ISSUER=AppTemplate
JWT_AUDIENCE=AppTemplate

# Frontend URL (for CORS and password reset emails)
FRONTEND_URL=http://localhost:3000

# Spring Profile
SPRING_PROFILES_ACTIVE=development
